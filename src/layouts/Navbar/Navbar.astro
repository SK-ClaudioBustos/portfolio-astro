---
import MoonIcon from "@icons/dark-mode/moon.svg";
import LinksContainer from "./LinksContainer.astro";
import LangButton from "@components/ui/LangButton.astro";
---

<nav class="navbar">
    <div class="navbar-content">
        <LinksContainer />
        <div class="buttons">
            <LangButton/>
            <MoonIcon />
        </div>
    </div>
</nav>

<style>
    .navbar {
        z-index: 5;
        position: sticky;
        top: 0;
        width: 100%;
        background-color: var(--c1);
        border-bottom: 2px solid var(--c2);

        & .navbar-content {
            height: 70px;
            max-width: 1980px;
            padding: 0px 25px;

            display: flex;
            justify-content: space-between;
            align-items: center;

            & .buttons {
                width: 80px;
                display: flex;
                align-items: center;
                gap: 2rem;

                font-size: 24px;
            }
        }
    }
</style>
